apiVersion: v1
kind: ConfigMap
metadata:
  name: env-config
data:
  POSTGRES_DB: postgres
  AWS_REGION: us-west-2
  AWS_BUCKET: mamatha-udagram-images
  AWS_PROFILE: default
  URL: http://localhost:8100
